import { Disclosure } from "@headlessui/react";
import { ChevronIcon } from "./ChevronIcon";

export const FAQ = () => (
  <div>
    <h3 className="text-4xl py-8">FAQ</h3>
    <div className="space-y-2">
      <Disclosure as="div" className="bordered-action">
        <Disclosure.Button className="flex items-center justify-between w-full space-x-4">
          <h4 className="text-lg text-left">Download Signer</h4>
          <ChevronIcon />
        </Disclosure.Button>
        <Disclosure.Panel>
          Metadata Portal is a self-hosted web page which shows you the latest
          metadata for a given network.Metadata Portal is a self-hosted web page
          which shows you the latest metadata for a given network.Metadata
          Portal is a self-hosted web page which shows you the latest metadata
          for a given network.Metadata Portal is a self-hosted web page which
          shows you the latest metadata for a given network.Metadata Portal is a
          self-hosted web page which shows you the latest metadata for a given
          network.
        </Disclosure.Panel>
      </Disclosure>
      <Disclosure as="div" className="bordered-action">
        <Disclosure.Button className="flex items-center justify-between w-full space-x-4">
          <h4 className="text-lg text-left">
            Signer Integration: How to Add a Network to Signer
          </h4>
          <ChevronIcon />
        </Disclosure.Button>
        <Disclosure.Panel>
          Metadata Portal is a self-hosted web page which shows you the latest
          metadata for a given network.Metadata Portal is a self-hosted web page
          which shows you the latest metadata for a given network.Metadata
          Portal is a self-hosted web page which shows you the latest metadata
          for a given network.Metadata Portal is a self-hosted web page which
          shows you the latest metadata for a given network.Metadata Portal is a
          self-hosted web page which shows you the latest metadata for a given
          network.
        </Disclosure.Panel>
      </Disclosure>
      <Disclosure as="div" className="bordered-action">
        <Disclosure.Button className="flex items-center justify-between w-full space-x-4">
          <h4 className="text-lg text-left">
            Signer Integration: How to Update Metadata in Signer
          </h4>
          <ChevronIcon />
        </Disclosure.Button>
        <Disclosure.Panel>
          Metadata Portal is a self-hosted web page which shows you the latest
          metadata for a given network.Metadata Portal is a self-hosted web page
          which shows you the latest metadata for a given network.Metadata
          Portal is a self-hosted web page which shows you the latest metadata
          for a given network.Metadata Portal is a self-hosted web page which
          shows you the latest metadata for a given network.Metadata Portal is a
          self-hosted web page which shows you the latest metadata for a given
          network.
        </Disclosure.Panel>
      </Disclosure>
      <Disclosure as="div" className="bordered-action">
        <Disclosure.Button className="flex items-center justify-between w-full space-x-4">
          <h4 className="text-lg text-left">
            How to add New Network and metadata to portal
          </h4>
          <ChevronIcon />
        </Disclosure.Button>
        <Disclosure.Panel>
          Metadata Portal is a self-hosted web page which shows you the latest
          metadata for a given network.Metadata Portal is a self-hosted web page
          which shows you the latest metadata for a given network.Metadata
          Portal is a self-hosted web page which shows you the latest metadata
          for a given network.Metadata Portal is a self-hosted web page which
          shows you the latest metadata for a given network.Metadata Portal is a
          self-hosted web page which shows you the latest metadata for a given
          network.
        </Disclosure.Panel>
      </Disclosure>
      <Disclosure as="div" className="bordered-action">
        <Disclosure.Button className="flex items-center justify-between w-full space-x-4">
          <h4 className="text-lg text-left">How to Fork Portal</h4>
          <ChevronIcon />
        </Disclosure.Button>
        <Disclosure.Panel>
          Metadata Portal is a self-hosted web page which shows you the latest
          metadata for a given network.Metadata Portal is a self-hosted web page
          which shows you the latest metadata for a given network.Metadata
          Portal is a self-hosted web page which shows you the latest metadata
          for a given network.Metadata Portal is a self-hosted web page which
          shows you the latest metadata for a given network.Metadata Portal is a
          self-hosted web page which shows you the latest metadata for a given
          network.
        </Disclosure.Panel>
      </Disclosure>
      <Disclosure as="div" className="bordered-action">
        <Disclosure.Button className="flex items-center justify-between w-full space-x-4">
          <h4 className="text-lg text-left">Existing Forks</h4>
          <ChevronIcon />
        </Disclosure.Button>
        <Disclosure.Panel>
          Metadata Portal is a self-hosted web page which shows you the latest
          metadata for a given network.Metadata Portal is a self-hosted web page
          which shows you the latest metadata for a given network.Metadata
          Portal is a self-hosted web page which shows you the latest metadata
          for a given network.Metadata Portal is a self-hosted web page which
          shows you the latest metadata for a given network.Metadata Portal is a
          self-hosted web page which shows you the latest metadata for a given
          network.
        </Disclosure.Panel>
      </Disclosure>
    </div>
  </div>
);
